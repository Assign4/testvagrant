describe('template spec', () => {
  it('Test to validate 4 foreign players in a team', () => {
    cy.visit('https://example.cypress.io')
  })

  it('Test to validate atleast one wicket keeper', () => {
    
  })
})